[project]
name = "rootstock-admin"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "modal>=1.3.5",
]

[dependency-groups]
develop = [
    "ruff>=0.15.5",
    "ty>=0.0.21",
]
